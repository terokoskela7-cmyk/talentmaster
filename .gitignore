# Lisää nämä rivit olemassaolevaan .gitignore-tiedostoon

# Firebase config — vain jos käytät eri avaimia eri ympäristöissä
# config.js        # ← poista kommentti JOS sinulla on .env.local -pohjainen flow

# Paikalliset ympäristömuuttujat
.env.local
.env.*.local

# Firebase emulaattorin data
.firebase/
firebase-debug.log
firestore-debug.log
ui-debug.log

# Testitulokset
coverage/
.nyc_output/

# Käyttöjärjestelmä
.DS_Store

# Tutkimusmateriaali (binäärit + mahdollisesti lisensoitu sisältö)
docs/research/

# Riippuvuudet (package-lock.json committaan, node_modules ei)
node_modules/
functions/node_modules/
